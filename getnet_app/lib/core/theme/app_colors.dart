import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFFE65100);       // Warm Orange/Amber
  static const Color primaryLight = Color(0xFFFFF3E0);  // Soft Warm Tint
  static const Color primaryDark = Color(0xFFBF360C);

  static const Color secondary = Color(0xFF2979FF);     // Soft Blue Accent
  static const Color success = Color(0xFF2E7D32);       // Goal Success Green
  static const Color warning = Color(0xFFF57F17);       // Budget Alert Warm Yellow

  // Neutral Colors
  static const Color background = Color(0xFFF8F9FA);    // Clean Soft Neutral
  static const Color surface = Color(0xFFFFFFFF);       // White Card Surfaces
  static const Color surfaceVariant = Color(0xFFF1F3F5);

  // Text Colors
  static const Color textPrimary = Color(0xFF1F2937);   // Dark Slate
  static const Color textSecondary = Color(0xFF6B7280); // Muted Slate
  static const Color textLight = Color(0xFF9CA3AF);

  // Border & Divider
  static const Color border = Color(0xFFE5E7EB);
  static const Color borderSelected = Color(0xFFE65100);
}
